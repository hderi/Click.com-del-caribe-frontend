"use client"; import { useEffect, useMemo, useState } from "react";
import { getToken } from "@/lib/authStorage";
import EquipmentHeader from "@/components/admin/EquipmentHeader";
import EquipmentTable from "@/components/admin/EquipmentTable"; const API_URL = process.env.NEXT_PUBLIC_API_URL || "http://localhost:3001"; function dateValue(value) { if (!value) return 0; const parsed = new Date(value).getTime(); return Number.isNaN(parsed) ? 0 : parsed;
} function formatDate(value) { if (!value) return "Sin historial"; const parsed = new Date(value); if (Number.isNaN(parsed.getTime())) return value; return parsed.toLocaleDateString("es-MX", { year: "numeric", month: "2-digit", day: "2-digit" });
} function repairDate(repair) { return repair.entregadoEn || repair.fechaIngreso || repair.creadoEn || repair.actualizadoEn || "";
} function isActiveRepair(repair) { return repair && repair.estado !== "entregado" && repair.linkActivo !== false;
} function belongsToEquipment(repair, item) { if (!repair || !item) return false; const repairEquipment = repair.equipo || {}; return repair.equipoId === item.id || repairEquipment.id === item.id;
} function normalizeEquipment(item, repairs) { const cliente = item.cliente || {}; const itemRepairs = repairs.filter((repair) => belongsToEquipment(repair, item)); const orderedRepairs = itemRepairs.slice().sort((a, b) => dateValue(repairDate(b)) - dateValue(repairDate(a))); const latest = orderedRepairs[0]; const active = itemRepairs.find(isActiveRepair); return { id: item.id || item.equipoId || item.serial || "SIN-ID", type: item.tipo || item.type || "Equipo", brand: item.marca || item.brand || "Sin marca", model: item.modelo || item.model || "Sin modelo", serial: item.numeroSerie || item.serie || item.serial || "Sin serie", tag: item.observaciones || item.observacionesRecepcion || item.tag || "Sin observaciones", client: cliente.nombre || latest?.cliente?.nombre || item.clienteNombre || item.client || "Sin cliente asignado", phone: cliente.telefono || latest?.cliente?.telefono || item.telefono || item.phone || "Sin teléfono", status: active ? "En taller" : latest?.estado === "entregado" ? "Entregado" : "Sin reparación activa", repairs: itemRepairs.length, currentFolio: active?.folio || latest?.folio || "", lastService: latest ? formatDate(repairDate(latest)) : formatDate(item.creadoEn), receivedBy: active?.recibidoPor || latest?.recibidoPor || "Sin registrar", };
} export default function EquiposPage() { const [searchQuery, setSearchQuery] = useState(""); const [equipment, setEquipment] = useState([]); const [repairs, setRepairs] = useState([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); useEffect(() => { let ignore = false; async function loadEquipment() { try { setLoading(true); setError(""); const token = typeof window !== "undefined" ? getToken() : ""; const headers = token ? { Authorization: `Bearer ${token}` } : {}; const [equipmentResponse, repairsResponse] = await Promise.all([ fetch(`${API_URL}/api/equipos`, { headers }), fetch(`${API_URL}/api/reparaciones`, { headers }), ]); const equipmentData = await equipmentResponse.json(); const repairsData = await repairsResponse.json(); if (!equipmentResponse.ok) throw new Error(equipmentData.error || "No se pudieron cargar los equipos"); if (!repairsResponse.ok) throw new Error(repairsData.error || "No se pudieron cargar las reparaciones"); if (!ignore) { setEquipment(equipmentData.equipos || []); setRepairs(repairsData.reparaciones || []); } } catch (err) { if (!ignore) { setError(err.message || "Error al cargar equipos"); setEquipment([]); setRepairs([]); } } finally { if (!ignore) setLoading(false); } } loadEquipment(); return () => { ignore = true; }; }, []); const equipmentRows = useMemo(() => equipment.map((item) => normalizeEquipment(item, repairs)), [equipment, repairs]); const filteredEquipment = useMemo(() => { const q = searchQuery.trim().toLowerCase(); if (!q) return equipmentRows; return equipmentRows.filter((item) => [item.type, item.brand, item.model, item.serial, item.client, item.phone, item.status, item.currentFolio, item.receivedBy] .join(" ") .toLowerCase() .includes(q) ); }, [equipmentRows, searchQuery]); const stats = useMemo(() => { const inShop = equipmentRows.filter((item) => item.status !== "Entregado" && item.status !== "Sin reparación activa").length; const delivered = equipmentRows.filter((item) => item.status === "Entregado").length; const history = equipmentRows.reduce((sum, item) => sum + Number(item.repairs || 0), 0); return [ { label: "Equipos", count: equipmentRows.length, color: "#0077B6", note: "Registrados" }, { label: "En taller", count: inShop, color: "#D97706", note: "Con folio activo" }, { label: "Entregados", count: delivered, color: "#1F8F5F", note: "Cerrados" }, { label: "Historial", count: history, color: "#475569", note: "Servicios acumulados" }, ]; }, [equipmentRows]); return ( <div className="space-y-6 "> <EquipmentHeader searchQuery={searchQuery} onSearchChange={setSearchQuery} equipmentCount={equipmentRows.length} /> {error && ( <div className="rounded-2xl border border-[#F3B5A7] bg-[#FFF2EE] px-4 py-3 text-sm font-black text-[#9A3412]"> {error} </div> )} <section className="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-4 gap-3"> {stats.map((item) => ( <div key={item.label} className="rounded-2xl border border-[#C9D8E5] bg-white p-4 shadow-[0_12px_28px_rgba(15,23,42,0.07)]"> <div className="flex items-start justify-between gap-3"> <div> <p className="text-xs font-black uppercase tracking-[0.12em] text-[#526174]">{item.label}</p> <p className="mt-1 text-3xl font-black" style={{ color: item.color }}>{item.count}</p> <p className="mt-1 text-xs leading-5 text-[#526174]">{item.note}</p> </div> <span className="mt-1 h-3 w-3 rounded-full" style={{ background: item.color, boxShadow: `0 0 0 6px ${item.color}18` }} /> </div> </div> ))} </section> <EquipmentTable equipment={filteredEquipment} loading={loading} /> </div> );
} 